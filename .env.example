# Groq API (https://console.groq.com/keys)
GROQ_API_KEY=

# Optional: HuggingFace token for gated model downloads (BGE is public, not strictly needed)
HF_TOKEN=

# Optional: where HuggingFace caches downloaded models (default: ~/.cache/huggingface)
HF_HOME=./.hf_cache